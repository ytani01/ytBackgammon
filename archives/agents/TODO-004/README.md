# TODO-004 の分担

`save_data()` がイベントループを止める時間を測った項目。

## 分担にした理由

- **測定スクリプトは main が書いて実行した。** 何を測るか（止まる時間の
  定義、履歴の長さの振り方）を決めるのと一体だったため
- **確認は verifier。** 測定は数字が判断の根拠になるので、独立に再現できる
  かどうかが要る。あわせて「測り方の穴」（`fsync()` を呼んでいない、
  組み立てと `write()` のどちらが支配的か、同じファイルへの書き込みで
  速く出ていないか）を明示して渡した
- **reviewer は使わなかった。** コードを 1 行も変えていないので、
  レビューする差分が無い

## ファイル

- `verifier-report.md` — 再現（3 回）と、測り方の穴の検証、
  実際の 1 手の流れ（`on_json` 経由）での測定、履歴の伸び方の調査

振り返りは
`archives/todo/TODO-004. save_data() のファイル I_O がイベントループを止める.md`
の「分担の振り返り」にある。
