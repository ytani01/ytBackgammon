# TODO-010 の分担

**プロトコルを一方向にするか決める。** 調べて決めるだけで、コードは変えない。

| 担当 | モデル | 役割 |
|------|--------|------|
| main | Opus 5 / effort high | 依頼、報告の裏取り、利用者との相談、決定 |
| Explore | Sonnet 5（組み込み） | `ytbg.js` の盤面更新ロジックの洗い出し |

## この分担にした理由

- `ytbg.js` は 4305 行あり、盤面更新ロジックの在処を洗い出すのは**事実を
  集めるだけで判断が要らない**。`Explore` に任せた
- `Explore` は `CLAUDE.md` を読み込まないので、読ませたいファイルを
  依頼文で名指しした（`ytbg.js`、`yt_backgammon.py`、
  `yt_backgammon_server.py`）
- コードを変えないので、確認とレビューの担当は置いていない。かわりに、
  **判断に効く事実は main が原文で裏を取った**

## 報告

- [explore-report.md](explore-report.md) — `load_gameinfo()` の完全性、
  ルール判定の所在、状態を書き換えるメソッドと描画するだけのメソッドの
  切り分け、クロックの扱い
