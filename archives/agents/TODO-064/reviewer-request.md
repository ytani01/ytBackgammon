# TODO-064 reviewer への依頼

## 目的

作業ツリーの未コミットの差分（`git diff` と未追跡のファイル）を、TODO-064 で決めたことと規約に照らしてレビューする。
コードは直さない。

## 読むもの

- `TODO.md` の TODO-064 の節（決めたこと）
- `archives/agents/TODO-064/implementer-request.md`（依頼）と `implementer-report.md`（実装の報告）
- `docs/Developer.md`、`CLAUDE.md`（書き方の慣習とテストの規約）

## 見てほしい点

- prefix の正規化と検査: 弾くべきものを通していないか、通すべきものを弾いていないか。CLI と設定で同じ関数を使っているか
- ルート: prefix の有無で `/`・`/foo`・`/foo/`・`/foo/p1`・`/foo/ws`・`/foo/static/...`・lobby の `/foo/api/...` が
  決めたとおりに応えるか。prefix 無しの振る舞いが今までと変わっていないか
- JS の URL の組み立て（`import.meta.url` からの相対）: `/`・`/p1`・`/foo/`・`/foo/p1` のどれから開いても正しいか。
  Node で読むテストに影響しないか。3 か月後に読んで分かるか（コメント）
- lobby: 子プロセスへ `--prefix` を渡すか。`url` のパスだけの値と `/\`・`//` の扱い。`url` を省いたときの URL
- テンプレートに埋め込む値が HTML / JS を壊さないか（prefix の文字の制限で足りているか）
- テスト: 決めたことを実際に固定しているか。壊したら落ちるか（報告の「壊して確かめたこと」が妥当か）
- 文書: Admin.md の nginx の例が決めた前提（パスを外さない）と合っているか。TODO の番号を書いていないか
- 過剰な実装が無いか

作業ツリーのファイルは書き換えない（`src/` を壊して試すのもしない。verifier が後で行う）。
サーバを起動して試すのはよい（ポートは空いているものを使い、終わったら PID を確かめて止める。`pkill` は使わない）。

## 報告

`archives/agents/TODO-064/reviewer-report.md` に、指摘を重さの順に（ファイルと行、何が起きるか、直し方の案）書く。
返事は「終わったか・報告ファイルのパス・判断が要る点」の 5 行以内。
