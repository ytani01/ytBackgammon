# TODO-040 の分担

| 担当 | 何を頼んだか |
|------|--------------|
| main | `__main__.py` の既定値を `images1a` に直す（1 行） |
| verifier | 検証一式と、**`-i` なしで実際に起動して画像が 200 で返ること**の確認 |

1 行の変更だが、既定値で起動できるようになるという挙動の変化なので、
確認を別の担当に分けた。実装した本人は diff を読んで「直っているはず」で
済ませてしまうため。

- [verifier-report.md](verifier-report.md)

項目そのものは
[archives/todo/TODO-040. `-i` の既定値に対応するディレクトリが無い.md](../../todo/TODO-040.%20%60-i%60%20の既定値に対応するディレクトリが無い.md)。
