# TODO-015 の分担

サーバからの受信を `gameinfo` 1 本にまとめた項目。

## 分担にした理由

- **実装を implementer に分けた。** `ytbg.js` と `yt_backgammon_server.py` の
  両方が変わり、テストと文書もまとまって要る（利用者の `CLAUDE.md`
  「複数のファイルにまたがる、実装とテストと文書がまとまって要る」）
- **確認は verifier。** 消した 13 分岐の行き先を 1 つずつ突き合わせ、
  壊しテストで実効性を確かめる担当
- **レビューも入れた。** 挙動が変わる項目で、しかも「サーバとクライアントの
  どちらにも状態がある」構造。定義のモデルは sonnet だが Opus 5 に上書きした

## ファイル

3 巡した。2 巡目以降は SendMessage で同じ担当を続けたので、依頼のファイルは無い。

- `implementer-report.md` — 実装（消した分岐とその行き先の対応表）と
  「## レビュー指摘の修正」
- `reviewer-report.md` — 1 巡目（要修正 2 件。うち無限ループは実測で特定）と
  「## 再レビュー（修正後）」（要修正 0 件、`Cube.double()` の副作用を発見）
- `verifier-report.md` — 1 巡目、「## 再確認（レビュー指摘の修正後）」、
  「## Cube.double() の修正の確認」

`Cube.double()` の修正だけは main が行い、確認を verifier に分けた（1 行）。

振り返りは
`archives/todo/TODO-015. サーバからの受信を gameinfo 1 本にまとめる.md`
の「分担の振り返り」にある。
