# TODO-012 の分担

`on_json()` のクロック系 5 分岐（中身が `pass` で綴りも誤っていたもの）を消す項目。

## 分担にした理由

- **実装は main。** `pass` だけの `if` を 5 つ消し、`CLAUDE.md` の該当記述を
  直しただけ。判断の要る箇所は無い
- **確認は verifier に分けた**（利用者の `CLAUDE.md`「コードやファイルを変える
  項目では、確認の担当を、項目の規模によらず必ず別のサブエージェントに分ける」）
- **レビューは置かない。** TODO.md の見込みどおり。到達不能だった分岐を消す
  だけで、条件式も分岐の意味も変わらない

## ファイル

- `verifier-task.md` / `verifier-report.md`
