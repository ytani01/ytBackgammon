# TODO-016 の分担

再接続したときにクロックの動作中／停止中が復元されない件。

## 分担にした理由

- **実装は main が持った。** 変更が `yt_backgammon_server.py` と `ytbg.js` の
  クロック周りに限られ、設計の判断（残り秒数まで戻すか）が実装と一体だったため
- **確認は verifier**（利用者の `CLAUDE.md`「コードやファイルを変える項目では、
  確認の担当を、項目の規模によらず必ず別のサブエージェントに分ける」）。
  `src/` をわざと壊して狙ったテストが落ちるかまで担当させた
- **レビューも入れた。** 挙動が変わる項目なので、「動くか」とは別に
  「良いか」を見る目が要る。クロックはサーバと `ytbg.js` の両方に計算が
  あり、ずれても自動テストでは出ない。定義のモデルは sonnet だが、
  Opus 5 に上書きした

## ファイル

2 巡した。2 巡目は SendMessage で同じ担当を続けたので、依頼のファイルは無い。

- `verifier-report.md` — 1 巡目（検証・ミューテーション 4 種）と
  「## 再確認（レビュー指摘の修正後）」（ミューテーション 3 種）
- `reviewer-report.md` — 1 巡目（要修正 2 件・検討 5 件）と
  「# 再レビュー（修正後）」（要修正 0 件）

振り返りは
`archives/todo/TODO-016. 再接続するとクロックの動作中／停止中が復元されない.md`
の「分担の振り返り」にある。
