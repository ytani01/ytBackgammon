# AGENTS.md（Codex 用）

## プロジェクトの指示

作業前に、このリポジトリの `CLAUDE.md` を最後まで読み、プロジェクトの
概要、構成、実行・検証方法、実装上の注意を Codex でも適用する。
利用者の直接の指示と、このファイルの Codex 向けの規則を優先する。
仕様の確認には現在のコードも使い、`archives/` は経緯の確認に使う。

## Claude Code との分離

- `CLAUDE.md`、`.claude/`、`~/.claude/` は参照だけに使う。
  利用者が対象を明示して変更を依頼した場合を除き、編集・移動・削除しない。
- Codex の指示は `AGENTS.md`、設定や hook は `.codex/` で保守する。
  Claude Code 用ファイルへのシンボリックリンクは作らない。
- `TODO.md` の既存項目に `CLAUDE.md` の更新が含まれていても、
  それだけで Claude Code 用ファイルを編集する許可とは扱わない。
  Codex 向けの補足はこのファイルへ書き、仕様の説明は `docs/` で保守する。
- Claude 固有のツール・担当・skill は、利用できる Codex の対応機能を
  確認してから使う。Claude のモデル名や料金・token 集計を流用しない。

## 作業と検証

- ソースコードの変更は、ユーザー全体の指示に従って `TODO.md` で管理する。
  既存項目の担当見込みを実施結果として使わず、実際の Codex の担当・モデル・
  reasoning effort を記録する。測定できない値は推測しない。
- 変更内容に応じて `CLAUDE.md` の検証方法を選び、実装者とは別の担当が
  確認する。確認・レビュー担当はコードを変更しない。
- `git push` は、利用者がその場で明示して依頼した場合だけ実行する。
